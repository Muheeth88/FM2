{
    "name": "puppeteer-framework",
    "version": "1.0.0",
    "scripts": {
        "test": "jest"
    },
    "devDependencies": {
        "puppeteer": "^{{PUPPETEER_VERSION}}",
        "jest": "^{{JEST_VERSION}}"
    }
}